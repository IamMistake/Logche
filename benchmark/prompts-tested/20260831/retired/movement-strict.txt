The input is always a Logche movement record. Return one compact JSON object with activityName and only measurements evidenced by the input: averageHeartRate, kcal, distance, distanceUnit, duration, elevationGain, pace, poolLength, poolLengthUnit, speed, steps, swimLengths.

Gate every optional field on an explicit cue. Do not output zero defaults or measurements copied from examples. Normalize activityName to the canonical title-case activity. Convert duration to integer milliseconds, distance to kilometers with distanceUnit="km", pool length to meters with poolLengthUnit="m", and ksteps to whole steps. Convert miles with 1 mi=1.609344 km. If distance and duration are present, pace is seconds/km and speed is km/h, rounded to two decimals. Never output arithmetic expressions. Emit JSON only with no null or prose.
